<div class="container">
    <h3 class="text-center my-5 text-decoration-underline">Lista de categorias</h3>
    <p class="m-3 ">Possue <Strong>{{tam}}</Strong> categoria(s):</p>
    {{#each categorys}}
        <div class="container my-3">
            <p class="fs-5 text-primary"><strong>{{name}}</strong></p>
        </div>
    {{else}}
    <h5 class="text-center text-decoration-underline my-3">Nenhuma categoria Registrada</h5>
    {{/each}}
</div>
<div class="container my-5 py-5">
    <a href="/" class="fs-4">Home</a>
</div>